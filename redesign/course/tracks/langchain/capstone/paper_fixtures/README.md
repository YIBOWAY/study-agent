# LC Capstone paper fixtures

Offline JSON papers for the LangChain track Capstone. Content may match the handwritten Capstone fixtures, but the path is independent—**do not import** `research_core` or handwritten Capstone modules.

Load via the Capstone agent or `json` + `PaperDoc` mapping in starter/solution code.
